#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * Return:Always 0
 */

int _abs(int)
{
	int ab;

	if (int < 0)
	{
		ab = int * -1;
		_putchar(ab);
	}
	else if (int > 0)
	{
		ab = int;
		_putchar(ab);
	}
	else
	{
		ab = 0;
		_putchar(ab);
	}

	return (0);
}

